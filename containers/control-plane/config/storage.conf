# control-plane-entrypoint rewrites this file at runtime with user-specific
# graphroot/runroot paths so that Podman storage remains stable across restarts.
[storage]
driver = "overlay"

[storage.options]
mount_program = "/usr/bin/fuse-overlayfs"
