apiVersion: v1
kind: ConfigMap
metadata:
  name: control-plane-config
  namespace: copilot-sandbox
data:
  # The entrypoint writes this object into the ephemeral runtime
  # ~/.copilot/config.json on each startup.
  copilot-config.json: |
    {
      "telemetry": false
    }
