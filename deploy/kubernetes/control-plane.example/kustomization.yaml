apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Shared PVCs are applied once from `install/shared-persistent-volume-claims.yaml`
# so routine `kubectl apply -k` does not try to mutate bound claim specs.
resources:
  - common/shared-resources.yaml
  - base/control-plane-instance.yaml
